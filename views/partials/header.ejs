<header id="top-header">
    <span class="siteName">
        <a id="title-link" href="/" title="Return to home page">CSE Motors</a>
    </span>
    <div id="tools">
        <% if (locals.isLoggedIn) { %>
            <a id="user-link" href="/account/">Welcome <%= locals.userName %></a>
            <a id="logout-link" href="/account/logout">Logout</a>
        <% } else { %>
            <a id="title-link" title="Click to log in" href="/account/login">MY ACCOUNT</a>
        <% } %>
    </div>
</header>